var firstName   = 'John';
var lastName    = 'Stewart';

var nameUppercase       = firstName.toUpperCase();
var lastNameLowercase   = lastName.toLowerCase();
var firstLetter         = firstName.charAt(0);
var amountLetters       = firstName.length;
var fullName            = firstName + ' ' + lastName;
var fullName2           = `${firstName} ${lastName.toUpperCase()}`;
var str                 = firstName.substr(1, 2);
